import momentShims from 'dummy/utils/moment-shims';
import { module, test } from 'qunit';

module('Unit | Utility | moment shims');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = momentShims();
  assert.ok(result);
});
