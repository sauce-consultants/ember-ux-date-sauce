import formattedDateRange from 'dummy/utils/formatted-date-range';
import { module, test } from 'qunit';

module('Unit | Utility | formatted date range');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formattedDateRange();
  assert.ok(result);
});
